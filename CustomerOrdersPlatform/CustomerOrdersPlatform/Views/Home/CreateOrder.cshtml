<div ng-controller="CreateOrderController">
    <h1>Create Order</h1>
    <select ng-model="selectedUser">
        <option ng-repeat="Customer in Customers" value="{{Customers._Customer_ID}}">
            {{Customer.First_Name}} {{Customer.Last_Name}}
        </option>
    </select>
 
    <h2>Products</h2>
    <table>
        <tr>
            <th>SKU</th>
            <th>Name</th>
            <th>Description</th>
            <th>Price</th>
        </tr>
        <tr ng-repeat="Product in Products" ng-click="setSelected(Product.id)" ng-class="{selected: Product.id === idSelectedProduct}">
            <td>{{Product.SKU}}</td>
            <td>{{Product.Name}}</td>
            <td>{{Product.Description}}</td>
            <td>{{Product.Price}}</td>
        </tr>
    </table>
    <br/>
    <div>
        <h2>Current Order</h2>
        {{SelectedProduct.Name}}
    </div>
    <div>
        <div layout="row">
            <button>Clear</button>
            <button ng-click="CreateOrder()">Save</button>
        </div>
    </div>
</div>

